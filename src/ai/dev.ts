
import { config } from 'dotenv';
config();

// This file previously imported Genkit flows for development purposes.
// As flows are being refactored into runtime-agnostic tasks,
// those imports are no longer needed here.
// This file can be used for other Genkit development initializations if required.
// For now, it's kept minimal.

// Example: If you still had Genkit tools or other Genkit-specific dev setups, they might go here.
// import '@/ai/tools/fetch-url-content-tool';
